name: publish

on: [push]
jobs: 
  publish-hello-world-image:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v2
  - name: Build the reunion Docker image
    run: |
        echo $CR_PAT | docker pull piyushgautamtg/reunion
    env:
      CR_PAT: ${{ secrets.CR_PAT }}